load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "backend",
    srcs = glob([
        "*.h",
        "*.cpp",
    ]),
    hdrs = glob(["*.h"]),
    visibility = ["//visibility:public"],
    deps = [
        "@imguilib//:imgui",
        "@spdloglib//:spdlog",
    ],
)
