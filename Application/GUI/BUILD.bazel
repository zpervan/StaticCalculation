load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "gui",
    srcs = glob([
        "Components/*",
        "*.cpp",
    ]),
    hdrs = glob(["*.h"]),
    visibility = ["//visibility:public"],
    deps = [
        "//Application/Core:core",
        "@imguilib//:imgui",
        "@spdloglib//:spdlog",
    ],
)
